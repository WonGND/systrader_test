# M1 게이트 보고 — 크롤러 + 원문 로컬 아카이브

- 작성일: 2026-08-26
- 마일스톤: M1
- 상태: **사용자 승인 대기**

---

## 1. 완료 항목

| 항목 | 결과 |
|---|---|
| 목록/본문 수집 | 완료 — 두 카테고리 전량 353건 |
| 재개 가능성 | 검증됨 — 2단계 실행이 1단계 수집분 10건을 자동 skip (`skipped 5` × 2) |
| 이미지 유무 판별 | 완료 — 본문 컨테이너 스코프로 `image_count`·`image_srcs` 인덱스 기록 |
| 인덱스 생성 | 완료 — `data/archive/index.jsonl` (로컬 전용, 커밋 안 됨) |
| rate limit | 준수 — 요청 간 2초 하한, 총 363요청 / 7,717초 (평균 21.3초/요청) |
| robots.txt 준수 | 전 요청 robots 대조, 차단 신호(403/429) 시 즉시 중단 로직 |

## 2. 수집 결과 (실측 수치)

| 카테고리 | 표시 글 수 | 수집 | 아카이브 | 실패 | 자가 검증 |
|---|---|---|---|---|---|
| 실전 투자 전략 (strategy) | 172 | **172** | 172 (167+검증분 5) | **0** | **MATCH** |
| 투자의 기초 (basics) | 181 | **181** | 181 (176+검증분 5) | **0** | **MATCH** |
| 합계 | 353 | 353 | 353 | 0 | — |

- 지시서 예상치 "약 172 / 약 181"과 **정확히 일치**. 보정 없음.
- 위젯 크롬으로 걸러진 링크: strategy 20개 / basics 21개.
- 위젯 노출 소속글 복원: strategy 4건 / basics 3건 — 1단계 검증 실행에서 발견된
  부족분(168/172, 178/181)과 정확히 일치. 복원 URL 전체는 실행 로그에 기록됨.

## 3. 검증 근거

1. **1단계 검증 실행** (`--limit 5`): 파싱 품질 확인 — 10건 아카이브, 실패 0,
   한글 제목·ISO 날짜 정상. 이때 수집 수 부족(168/172, 178/181)을 발견.
2. **필터 v2 수정**: 원인은 "카테고리 소속이면서 사이드바 위젯에 상시 노출되는 글"의
   오분류. 페이지별 실제 목록 컨테이너를 실측해 복원하는 로직 추가.
   재현 테스트 포함 오프라인 단위 테스트 **7/7 통과** (`tests/crawler/test_parsers.py`).
3. **2단계 전량 실행**: 두 카테고리 모두 MATCH, 실패 0. 전체 로그는 사용자 로컬 실행
   출력으로 확인(본 문서 §2 수치가 그 SUMMARY).
4. **아카이브 상세 통계**: `python -m src.extractor.index_stats` 출력으로 확인
   (이미지 포함 글 비율, 연도별 분포, 텍스트 길이, 중복 해시 등 — §7 참조).

## 4. 발견된 문제와 한계

| # | 내용 | 처리 |
|---|---|---|
| 1 | 사이드바 위젯 오염으로 목록 필터 v1이 소속글 7건 누락 | 필터 v2로 해결, MATCH 확인. DECISIONS.md 기록 |
| 2 | 실행 시간이 예상(15~20분)보다 긴 2시간 9분 — 평균 요청 간격 21.3초(2초 하한 + 서버 응답/전송 시간) | 문제 아님(보수적일수록 안전). 향후 배치 예상치는 실측 기준으로 산정 |
| 3 | 복원된 글 중 국내 시장 제목 존재(예: "코스닥 지수를 이용한 종가 베팅…") | 정상 — 수집은 전량, 트랙 분류(A/B/C)는 M2에서 수행 |
| 4 | 원문은 사용자 로컬 `data/archive/`에만 존재(원문 비공개 원칙). 원격 세션은 원문을 직접 읽을 수 없음 | M2 설계에 반영: 파생 메타데이터(제목·키워드 특징)는 커밋 가능, 본문 분석이 필요한 후보는 사용자 경유로 전달 (§6) |

## 5. 미결 질문 (권장안 포함)

M1 종결 자체의 블로킹 질문은 없음. M2 착수 전 확인 1건:

| # | 질문 | 권장안 |
|---|---|---|
| Q6 | M2에서 원격 세션이 원문을 읽을 수 없는 문제의 처리 방식은? | **2단계 깔때기**: ① 로컬 스크립트가 커밋 가능한 파생 특징(제목, 전략 키워드 매칭, 수치·파라미터 밀도, 이미지 수, 연도)을 생성 → 원격에서 1차 후보 선별 ② 상위 후보(~30개)의 본문 텍스트만 사용자가 세션에 전달(채팅 첨부, 저장소 커밋 아님) → 정독 후 후보 15개 표 작성. 원문 비공개 원칙(저장소 기준)과 근거 인용 의무를 모두 충족 |

## 6. 다음 단계 계획 (M2)

1. `src/extractor/features.py`: 로컬 실행용 파생 특징 생성기
   (전략 키워드 사전, 파라미터 패턴 밀도, 수익률 수치 존재, 표/코드 블록 유무 등)
   → `data/specs/post_features.jsonl` **커밋 가능**(메타데이터·파생물만)
2. 원격에서 특징 기반 1차 스크리닝 → 본문 정독 대상 shortlist(~30) 제시
3. 사용자: shortlist 본문 텍스트 전달 (`data/archive/posts/*.txt`에서)
4. 정독 후 **후보 15개 표** 제시 (트랙/전략유형/survivorship_risk/M4 재현 가능/
   치환 건수/화이트리스트 밖 치환 유무 + **Track A 실제 개수 명시**)
5. 사용자가 10개 확정 → M2 종료, M3(엔진) 착수

## 7. 아카이브 통계 (index_stats 출력, 2026-08-26 사용자 로컬 실행)

```
total unique posts: 353
  basics: 181 (advertised 181)
  strategy: 172 (advertised 172)
duplicate content hashes: 0
posts missing published_at: 0
posts per year:
  2014: 40 / 2016: 35 / 2017: 57 / 2018: 33 / 2019: 36 / 2020: 30
  2021: 46 / 2022: 13 / 2023: 17 / 2024: 29 / 2025: 17
posts with >=1 image: 353 (100.0%)
image count buckets: 0: 0 / 1-2: 49 / 3-5: 113 / 6-10: 158 / >10: 33
text length (chars): min 543, median 3908, mean 4608, max 27800
posts with text < 500 chars: 0
content selector usage: .tt_article_useless_p_margin 344, .contents_style 9
numeric-URL posts: 0
```

**해석 (M2에 반영)**
- 중복 0, 날짜 결측 0, 최소 본문 543자 — 아카이브 품질 양호. 폴백 셀렉터
  `.contents_style` 사용 9건은 파싱 성공이며 별도 조치 불요.
- **전 글 100%가 이미지 포함** → `needs_image_review` 판별은 이미지 유무가 아니라
  "텍스트만으로 파라미터·규칙이 완결되는가"로 판단해야 한다(Q5 보수 원칙 그대로).
- **2015년 글 0건** (2014: 40 → 2016: 35). 실측 사실로 기록, 보정하지 않음.
- 인샘플 경계(2018-12-31) 이전 글 165건 / 이후(2019~) 188건.

## 8. 승인 기록

- **2026-08-26 사용자 승인** ("추천대로 진행해줘"): M1 게이트 종결 + Q6 권장안
  (2단계 깔때기) 채택. M2 착수.
