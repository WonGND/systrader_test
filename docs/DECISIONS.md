# DECISIONS

기술적·방법론적 결정의 영구 기록. 형식은 CLAUDE.md §8을 따른다.

`날짜 | 결정 | 근거 | 검토한 대안 | 되돌릴 조건`

---

| 날짜 | 결정 | 근거 | 검토한 대안 | 되돌릴 조건 |
|---|---|---|---|---|
| 2026-08-31 | **M5 판정을 §7 v1.0 그대로 기록**한다(alive 0 / weak 14 / dead 3). 전 런이 기준 (3)에서 탈락했다는 사실을 근거로 기준을 사후 조정하지 않는다. 위험조정 비교(Sharpe·MDD 대 SPY)는 **보조 정보**로만 병기 | 결과를 본 뒤 임계값을 고치면 검정이 사후 합리화가 된다. 기준 (3)이 절대 CAGR을 100% 주식과 비교한다는 성질은 기준 제정 시점에 이미 알 수 있었던 설계 속성이며 이번 구간의 우연이 아니다 | (3)을 "Sharpe ≥ SPY Sharpe"로 대체한 v1.1 즉시 적용(사후 조정), 판정 자체 보류(정보 손실) | 사용자가 v1.1을 지시할 때 — 버전업 + 전 런 재판정 + v1.0/v1.1 병기 |
| 2026-08-31 | **L-09(OOS 오염) 등재**: 발행일이 2019-01-01 이후인 글(C2·C6·C7·C8·C9)은 진정한 OOS가 아니므로 해석에서 할인한다. 단 **판정 구간은 2019-01-01 고정**(§5 전략별 임의 조정 금지)이며 발행일 기준 절단은 보조 정보로만 산출 | §5가 인샘플/OOS 경계의 전략별 조정을 금지한다. 오염 사실을 숨기는 것도 §4 은폐 금지 위반이므로 판정은 유지하되 한계로 명시 | 전략별 OOS 시작일을 발행일로 이동(§5 위반, 전략 간 비교 불가), 오염 전략 판정 제외(정보 손실) | §7 개정 시 오염 처리 규칙을 기준에 편입할 때 |
| 2026-08-31 | **M4 게이트 승인 종결** (사용자: "추천대로 진행해줘"). Q9 승인(엔진 검증 통과 기준 종결) / Q10 그리드 서치 미실행 / Q11 판정 지표 일간 고정 / Q12 M5에 C13(Track B) 포함 | `reports/M4_inSample_reproduction.md` §5 권장안 그대로 승인 | 각 질문의 대안은 보고서 §5에 병기 | 사용자 재지시 시 |
| 2026-08-31 | **M5 판정은 비용반영 일간 지표로 수행**하고 벤치마크 SPY도 동일 비용을 적용(§7 "동일 기간·동일 비용"). 무비용 수치는 참고로 병기 | 판정 질문이 "실제로 살아있는가"이므로 비용 차감 후가 옳다. 벤치마크에만 비용을 면제하면 (3) 기준이 왜곡됨 | 무비용 기준 판정(고회전 전략에 유리한 편향), 비용 무시(현실성 상실) | 판정 기준(§7) 버전업 시 |
| 2026-08-31 | **M5는 전략별로 전체 기간을 한 번만 백테스트하고 지표를 인샘플/OOS로 절단**한다. 2019년에 엔진을 재시작하지 않는다 | 재시작하면 2018년부터 보유 중이던 포지션이 강제 청산되고 존재하지 않던 거래가 만들어진다. OOS MDD는 OOS 시작 시점을 기준 고점으로 계산 | 창별 독립 실행(경계 인공물 발생), 경계에서 상태 수동 이월(복잡·오류 소지) | 상태 이월 없는 전략만 다루게 될 때 |
| 2026-08-31 | **워크포워드는 롤링 36개월 안정성 점검으로 수행**하고 파라미터 재적합은 하지 않는다 | 본 프로젝트의 모든 파라미터는 원문 고정값이라 재적합 대상이 없다. 재적합은 §1-1(무추측) 위반이자 인샘플 과최적화 주입 | 파라미터 재최적화 워크포워드(원칙 위반), 워크포워드 생략(성과 편중 여부 미확인) | 원문이 파라미터 탐색을 지시하는 전략을 다루게 될 때 |
| 2026-08-31 | **C13 이식판 구현 확정**: 위험 [SPY, EFA] 상대 모멘텀 상위 1 → 12개월 평균 모멘텀 스코어로 비중 산정(위험:안전 1:1) → 잔여분 SHY → 6개월 수익곡선 모멘텀 하락 시 전액 SHY. 안전자산군 스코어 적용판은 대조 변형으로 병기 | 스펙 c13의 5단계 인용 그대로. 3단계 인용이 "선정된 종목" 대상이라 안전자산군 스케일링은 본안에서 제외하고 대조로 남김 | 안전자산군에도 스코어 적용(원문 근거 없음 — 대조판으로 강등), 게이트 시 0% 현금(유니버스에 현금 자산 SHY가 지정돼 있어 기각) | 원문 후속 글에서 4·5단계 세부가 확인될 때 |
| 2026-08-31 | `config/backtest_defaults.yaml`의 `auto_adjust: null` → `true`로 정정(코드값과 일치). 회귀 테스트로 config-코드 일치를 강제 | M3에서 True로 확정(L-05)했으나 config가 갱신되지 않아 문서-코드 불일치 상태였다. §5는 규약을 config 단일 출처로 요구 | 코드에서 config를 읽어 주입(어댑터 초기화 순서 복잡도 대비 이득 낮음 — 대신 일치 테스트로 고정) | 어댑터가 config를 직접 읽도록 리팩터링할 때 |
| 2026-08-31 | **M4를 "원문 수치 재현"이 아니라 "엔진 검증 통과" 기준으로 종결 제안** (`reports/M4_inSample_reproduction.md`). 원문과의 잔여 격차는 규약(월간)·기간(2019+ 포함)·미명시 파라미터로 분해해 한계(L-06/07/08)로 등재하고, 추측 보정은 하지 않음 | CLAUDE.md §0이 재현을 목적이 아닌 수단으로 정의. 원문 자체로 검증 가능한 항목(C2 잔여분 산식, SPY MDD 0.507848)은 일치 | 원문 수치 일치까지 파라미터 탐색(§1-1 위반 + 인샘플 과최적화 주입), 격차 미기재 종결(은폐) | 원문 후속 글에서 미명시 파라미터가 확인될 때 해당 전략만 재실행 |
| 2026-08-31 | **C6·C9의 원문 격차를 그대로 보고**하고 그리드 서치로 맞추지 않음. C9는 300일선을 원문 서술대로 청산 조건으로만 적용한 판을 본안으로, 진입 필터 적용판을 가정 변형으로 병기 | §1-1 무추측. 진입 필터판(Sharpe 0.42)이 본안(0.64)보다 나쁘므로 "필터 해석 실수" 방향으로 격차가 설명되지 않음 | 파라미터 탐색(원칙 위반), 전략 제외(정보 손실) | 원문/원저자 코드에서 포지션 크기·체결 시점이 확인될 때 |
| 2026-08-30 | **미배분 잔여 비중을 유니버스 내 현금 자산으로 라우팅** (C1→SHY, C2 영구→AGG). 0% 현금에 두던 기존 구현은 대조판으로 남겨 효과를 가시화 | 원문 (72)의 계산 예시가 잔여 40%를 현금 라인에 가산해 최종 17.5/12.5/5/**65**를 제시 — 원문 근거 있는 수정 | 잔여를 0% 현금 유지(구조적 과소평가: c01 -0.54%p, c02 -1.39%p), 잔여를 전 자산 비례 배분(원문 근거 없음) | 다른 글에서 잔여분 처리를 달리 명시할 때 그 글만 별도 적용 |
| 2026-08-30 | **C2 올웨더는 잔여 라우팅 미적용** — 유니버스에 '현금' 라벨 자산이 없어 잔여는 0% 현금. 가정값으로 표기 | 원문이 올웨더 구성의 잔여 처리를 명시하지 않음. 임의로 IEF/TLT에 가산하면 추측 | DBC/IEF 중 임의 선택(근거 없음) | 원문에서 올웨더 현금 라인이 확인될 때 |
| 2026-08-30 | **엔진 내부 루프 numpy 벡터화** (슬리브 런 9.4s → 0.20s). 회귀 18건으로 동작 불변 확인 후 교체 | C5는 75슬리브 × 2(비용)로 순수 파이썬 루프 시 실행 시간이 실용 범위를 벗어남 | 슬리브 수 축소(전략 정의 훼손), 병렬 프로세스(복잡도) | 벡터화 경로에서 결과 불일치가 관측될 때 |
| 2026-08-07 | `config/backtest_defaults.yaml` v1.0 생성. 값은 CLAUDE.md §5 명시값 그대로(익일 시가, 수수료 0%, 편도 슬리피지 0.05%, 거래세 0, 인샘플/OOS 경계, SPY 벤치마크). `auto_adjust`는 null로 두고 M3에서 확정 | CLAUDE.md §2가 이 파일을 요구하며 §5가 "config로 파라미터화, 하드코딩 금지"를 명시. 새 값 창작 없음 — 전부 §5 인용 | 엔진 구현 시점(M3)에 생성 — 그 전까지 규약이 문서에만 존재해 코드와 어긋날 위험이 있어 기각 | §5 규약 자체가 사용자 지시로 변경될 때(버전업 동반) |
| 2026-08-26 | **M1 게이트 승인 종결** (사용자: "추천대로 진행해줘"). 353건 아카이브, 자가 검증 172/172·181/181 MATCH, 실패 0. 동시에 Q6 승인 — M2는 2단계 깔때기(파생 특징 1차 선별 → 상위 ~30개 본문만 세션 전달 → 정독 후 후보 15개) | 원문 비공개 원칙(저장소 커밋 금지)과 스펙 근거 인용 의무를 모두 충족하는 유일한 실행 가능 경로 | 전 글 본문 세션 전달(353건 과다), 원문 커밋(원칙 위반), 키워드만으로 스펙 작성(근거 인용 불가) | 사용자가 로컬 Claude Code 등 원문 직접 접근 수단을 도입할 때 |
| 2026-08-26 | **M2 파생 특징 스키마 확정**: `data/specs/post_features.jsonl`에 제목·키워드 그룹 히트수·해외/국내 힌트·명시 티커·파라미터 패턴 밀도·성과 용어 빈도만 기록. 본문 인용 미포함 → 커밋 가능. 키워드 히트는 **스크리닝 신호일 뿐** 스펙 필드에 사용 금지(스펙은 원문 인용 필수) | 커밋 가능한 파생물의 경계를 명확히 함. hard_constraint #1(무추측)과 #5(원문 비공개) 동시 준수 | 본문 요약 포함(비공개 원칙 경계 모호), 특징 없이 제목만(신호 부족) | 특징이 스크리닝에 불충분하다고 판명될 때(어휘 확장으로 대응) |
| 2026-08-26 | **원문 성과 지표는 월간(R/PerformanceAnalytics) 기준으로 확정** — 리포트에 일간/월간 병기 | C2 원문 표의 SPY Worst Drawdown 0.507848가 우리 SPY **월간** MDD -50.78%와 정확 일치(일간은 -55.19%, M3에서 외부 검증). Sharpe도 √12 vs √252로 스케일이 다름 | 일간만 보고(원문과 비교 불가), 원문 수치 무시 | 다른 글이 일간 산출임이 확인될 때 그 글만 별도 표기 |
| 2026-08-26 | **C2 원문 백테스트 기간 = 2005-11 ~ 2020-02 로 추정 확정** | 원문 Cumulative Return 1.7262(=+172.6%, 배수 2.726)와 연 7.25%가 14.25년에 정합. SPY 연 8.72%도 이 기간과 부합. 우리 자동 산출 시작일(2005-11-30)과 정확히 일치 | 기간 미상으로 두고 대조 포기 | 원문에 기간 명시가 발견될 때 |
| 2026-08-26 | **잔여 비중 처리 수정: 미배분분을 유니버스의 '현금' 자산에 가산** (C1→SHY, C2 영구→AGG). 기존 구현(수익률 0% 현금)은 대조판으로 병기 | 원문 (72)의 산식 예시가 명시적: 스코어 0.7/0.5/0.2/1 → 17.5/12.5/5/25(합 60%), "40%가 남는 비중은 현금으로 대치(현금 비중에 추가)" → 최종 17.5/12.5/5/**65**. 우리 구현은 이 65%를 0% 현금으로 처리해 수익을 구조적으로 과소평가 | 0% 현금 유지(원문 산식과 불일치) | 원문 재해석이 필요하다는 근거가 나올 때 |
| 2026-08-26 | **C2 올웨더는 잔여분을 0% 현금으로 유지** | 올웨더 유니버스(주식/중기채/장기채/금/상품)에는 '현금' 라벨 자산이 없어 가산 대상이 원문에 없음 → 추측 금지 원칙상 가정값으로 표기 | AGG/IEF 등에 임의 가산(근거 없음) | 원문에 현금 자산 지정이 확인될 때 |
| 2026-08-26 | **M3 게이트 승인 종결** (사용자: "M3 승인할게"). 합성 회귀 11 + 크롤러 7 = 18/18, 실데이터 SPY B&H·60/40 외부 대조 통과 | 회귀 전 항목 통과가 §6 요구 조건 | — | — |
| 2026-08-26 | **엔진 내부 루프 numpy 재작성** (로직·인터페이스 불변). 단일 티커 4,700일 런 9.41s → 0.20s (47배) | C5는 75슬리브 × 3런 = 225런이라 기존 속도로는 실행 불가(35분+). 회귀 18건이 동작 불변을 보증 | 슬리브 수 축소(원문 훼손), 전략별 벡터화(중복 코드) | — |
| 2026-08-26 | **C4/C5/C7 슬리브 결합 방식 = 등가 자본 최초 분할 후 슬리브 간 리밸런싱 없음**. 각 슬리브를 독립 백테스트한 뒤 수익곡선 평균 | 원문 코드가 `a.mean(axis=1)`로 독립 수익곡선을 평균 — 슬리브 간 주기적 리밸런싱은 원문에 없음 | 매일 목표비중 벡터로 통합 리밸런싱(원문과 다른 전략이 됨) | 원문에 리밸런싱 명시가 확인될 때 |
| 2026-08-26 | **C6 모멘텀 해석 = 1·3·6·12개월 수익률의 평균**(Keller HAA 정의). 원문 표현은 '평균 모멘텀 스코어'이나 '0 이하' 비교가 성립하는 해석을 채택, 가정값으로 표기 | 스코어(0~1)는 '0 이하'가 사실상 전부 음수인 경우만 의미 → 원문이 링크한 allocatesmartly HAA 정의와 불일치 | 스코어 해석(카나리아가 거의 발동하지 않음) — M4 결과 검토 후 재고 가능 | M4 결과가 원문 주장(연 15%/MDD -10%대)과 크게 어긋나면 대안 해석 병행 검증 |
| 2026-08-26 | **엔진 메트릭 정의 확정**: CAGR=캘린더일 기준 (end/start)^(365.25/days)-1, Sharpe=일간 평균/표준편차×√252 (rf=0), 승률=시장 노출일 중 양(+)일 비율, 회전율=편도 거래대금/포트폴리오가치. 코드·리포트에 정의 명기 | 지표 정의가 다르면 외부 수치 대조가 무의미해짐. 실데이터 대조에서 공개 SPY 수치와 일치 확인 | 거래일 기준 CAGR(252), rf 반영 Sharpe(무위험수익률 시계열 추가 필요) | 외부 대조 기준이 바뀔 때(버전업 동반) |
| 2026-08-26 | **합성 회귀 시세에 오버나이트 갭 도입**: 기존 생성기(시가=전일종가)는 시가체결/종가체결을 수치적으로 구분하지 못해 결함을 은폐. 갭 생성기 + 판별력 검사(두 방식 차이 >1e-4)를 추가 | M3 실데이터 실행의 FAIL(검증식 매수단가 오류)이 합성 테스트를 통과했던 사실이 사각지대를 입증 | 실데이터로만 검증(원격 세션 네트워크 차단으로 불가) | — |
| 2026-08-26 | **M2 게이트 승인 종결** (사용자: "게이트 종결 후 M3으로 가자"). 스펙 10건 + content_hash 충전(10/10, 인덱스 불일치 0) 확인 후 종결 | 원격 재검증: 해시 전부 유효 sha256, 스키마 0오류 | — | — |
| 2026-08-26 | **가격 조정 방식 확정: yfinance `auto_adjust=True`** (수정종가 = 배당 재투자 총수익 근사). 모든 시그널·수익 계산에 동일 시계열 사용. 코드·리포트에 명시 (L-05 갱신) | CLAUDE.md §4 "배당 포함 총수익 기준" 이행의 표준 방법 | adjusted close 별도 컬럼 사용(구버전 yfinance 호환성 낮음), 배당 수기 재투자(복잡도 대비 이득 없음) | 시그널이 미조정가 의존(전일 고가 비교 등)으로 왜곡된다는 증거 발견 시 — 해당 전략만 미조정가 병용 검토 |
| 2026-08-26 | **M2 대표 전략 10개 확정** (사용자: "응 다 권장하는대로 해줘"): C1(평균모멘텀스코어 배분 14+17 병합), C2(동적 영구/올웨더 72), C3(Accelerating DM 60), C4(RSI2 역추세 39), C5(다중 이평 돌파 35), C6(HAA 136), C7(계절성 115), C8(QQQ/TLT 스프레드), C9(IBS 하한선), C13(MPAA 31 이식) | 후보 15 표 기반, Track A 9 + B 1, 4대 유형 커버, M4 텍스트 대조 4건 | C10(중복), C11(숏 부담), C12(OOS 오염), C14(WL 밖 치환 다수), C15(정체성 훼손 소지) | 사용자 재지시 시 |
| 2026-08-26 | **Q7 승인**: HAA 공격형 8자산을 Keller 원논문 유니버스(SPY,IWM,EFA,EEM,VNQ,DBC,IEF,TLT)로 채움 — 스펙에 assumption_needed=true + 원문 비인용 명기 | 원문에 목록 부재. 원논문이 1차 출처 | 채택 포기(유형 다양성 손실) | 원문 후속편에서 목록 확인 시 갱신 |
| 2026-08-26 | **Q8 승인**: 계절성(C7) SPY·TLT 동시 보유 시 50:50 분할 가정 — 리포트에 가정값 표기 | 원문 미명시, 대칭 분할이 최소 가정 | 신호 우선순위 부여(근거 없음) | 원문/원영상에서 배분 확인 시 |
| 2026-08-26 | **C13 이식 설계**: 위험자산=원문 예시 국가 지수만(S&P500→SPY, KOSPI200→SPY 흡수, Nikkei/Eurostoxx→EFA), 10년국고채→IEF, 3년국고채→SHY — 전부 whitelist. 국내 섹터·팩터 구성분은 WL 밖이라 제외. 상위 6/19(≈30%) 비율 유지 → 축소 유니버스 상위 1/2 | 화이트리스트 단일 진실 공급원 원칙. 비율 보존이 파라미터 임의 변경 최소화 | 섹터/팩터까지 user_approved 확장(질문 부담 대비 효익 낮음) | 사용자가 유니버스 확장 지시 시 |
| 2026-08-26 | **스펙 생성 파이프라인**: 모든 source_quote를 전달받은 번들 원문과 기계 대조(181건 검증) 후 기록. NBSP 등 공백 차이는 원문 쪽 문자열을 채택. content_hash는 로컬 인덱스에서 fill_spec_meta.py로 충전 | 근거 필수 규칙(무인용 값 무효)의 기계적 집행 | 수기 인용(오탈자 위험) | — |
| 2026-08-26 | **M1 크롤러 파싱 셀렉터 확정(실측 기반)**: 제목 `og:title`, 날짜 `meta[article:published_time]`(ISO), 본문 `.tt_article_useless_p_margin` → `.contents_style` 폴백. `.entry-content`는 기각. 셀렉터 불일치 시 폴백 추측 없이 ParseError로 실패 기록 | 2026-08-26 로컬 탐침 실측: og:title·published_time은 유일 매치, `span.date`는 21개 매치로 모호, `.entry-content`는 광고 텍스트("728x90 반응형") 혼입 | `span.date` 파싱(모호), `.entry-content`(오염) | 블로그 스킨 변경으로 셀렉터 매치 실패가 관측될 때(재탐침 후 갱신) |
| 2026-08-26 | **카테고리 목록 수집 방식**: `?page=N` 순회 + "여러 페이지 반복 링크 = 사이드바 위젯" 필터(과반 페이지 등장 시 제외) + 최종 수집 수를 표시 글 수(172/181)와 대조해 자가 검증. 불일치 시 보정 없이 보고 | 실측에서 페이지당 실제 ~28글인데 링크 54~55개 검출(위젯 오염). 목록 컨테이너 셀렉터는 미실측이라 추측 대신 통계적 필터 채택 | 목록 컨테이너 셀렉터 추측(원칙 위반), 글별 카테고리 재확인 크롤(요청 2배) | 자가 검증에서 172/181 불일치가 나올 때(사용자 보고 후 방식 재검토) |
| 2026-08-26 | **목록 필터 v2 — 위젯 노출 소속글 복원 로직 추가**: 페이지별 고유 링크의 80% 이상을 포함하는 최심(最深) 요소를 "실제 목록 컨테이너"로 실측 판정하고, 위젯으로 분류된 링크가 어떤 페이지의 그 컨테이너 안에 존재하면 소속글로 복원 | 1단계 검증 실행에서 168/172·178/181 부족 발견. 원인: 카테고리 소속이면서 사이드바 인기글/최근글 위젯에 상시 노출되는 글이 전 페이지 반복 등장 → 빈도 필터가 위젯으로 오분류. 컨테이너는 스킨 셀렉터 추측 없이 페이지 자체에서 측정 | 위젯 링크 전부를 글로 취급(타 카테고리 글 혼입), 컨테이너 셀렉터 하드코딩(추측 금지 위반), 부족분 방치(자가 검증 실패 상태로 전량 수집) | 복원 후에도 172/181 불일치가 나오면 사용자 보고 후 방식 재검토 |
| 2026-08-26 | **수집 대상 카테고리 URL 확정**: `/category/systrader79 칼럼/실전 투자 전략`(172), `/category/systrader79 칼럼/투자의 기초`(181). 상위 "systrader79 칼럼"(451)은 수집하지 않음 | 실측 글 수가 지시서 예상(172/181)과 정확히 일치. 상위 카테고리는 하위 합산(451=172+181+1+97)이라 중복 수집 | 상위 카테고리 전체 수집(범위 초과), "ETF 모델 포트폴리오"(97) 추가(지시서 범위 밖 — 필요 시 사용자 별도 승인) | 사용자가 수집 범위 변경을 지시할 때 |
| 2026-08-07 | **M0 미결 질문 Q1~Q5를 전부 권장안으로 확정** (사용자 답변: "추천대로"). Q1 로컬 Windows에서 크롤링 실행(원격은 코드만) / Q2 읽기 전용 탐침 `probe.py`로 구조 실측 / Q3 탐침이 카테고리 자동 열거 / Q4 HTML+텍스트 병행 저장·이미지 바이너리 미다운로드 / Q5 보수적 이미지 의존 판별+M2 사람 검토 | 원격 환경 네트워크 차단(B-01)과 gitignore로 인한 수집물 소실(P-04). 셀렉터 추측 금지 원칙 | 컨테이너 allowlist 추가 단독(환경 설정 권한 필요), 샘플 HTML 수동 전달 단독(페이지네이션 실측 불가) | 환경 allowlist에 대상 도메인이 추가되면 원격 실측·실행으로 전환 가능 |
| 2026-08-07 | 탐침 `probe.py` 설계 확정: 요청 상한 10회, 요청 간 최소 2초(하향 불가), 모든 URL을 robots.txt 대조 후 요청, robots 불허 시 즉시 중단, 본문 미저장(구조 메타데이터만 출력), 오프라인 selftest 내장 | hard_constraint #5(2초 지연)·CLAUDE.md §11(차단 시 우회 금지). selftest는 네트워크 없는 원격 환경에서 파서 검증 수단 | 상한 없는 전량 열거(과도한 요청), Selenium 동적 렌더링(불필요 복잡도) | 대상 스킨이 JS 렌더링 필수로 확인될 때 |
| 2026-08-07 | `requirements.txt` 추가 (requests, beautifulsoup4, lxml, jsonschema, PyYAML, numpy, pandas, yfinance, pytest; Python ≥3.10) | 재현 가능한 환경 구성. 기술적 세부 선택은 자체 결정 후 기록(CLAUDE.md §10) | conda env / pyproject.toml — 사용자 환경(Windows venv) 기준 requirements.txt가 가장 단순 | 패키지 충돌 발생 시 버전 핀 고정으로 전환 |

---

## 기록 규칙
- 사용자 승인이 필요한 결정은 승인 확인 후에만 기록한다.
- 판정 기준(CLAUDE.md §7) 변경 시 버전을 올리고 여기에 반드시 남긴다.
- 화이트리스트(`config/ticker_whitelist.yaml`) 항목 추가는 전부 여기에 기록한다.
